type GridItemBlock implements Block @key(fields: "id") @shareable {
  id: ID! @shareable
  journeyId: ID! @shareable
  parentBlockId: ID @shareable
  parentOrder: Int @shareable
  xl: Int! @shareable
  lg: Int! @shareable
  sm: Int! @shareable
}
