export {
  firebaseClient,
  getUserFromPayload,
  getUserIdFromPayload,
  auth,
  impersonateUser,
  sanitizeDisplayName,
  sanitizePhotoURL,
  splitDisplayName,
  type User,
  type UserRecord
} from './firebaseClient'
