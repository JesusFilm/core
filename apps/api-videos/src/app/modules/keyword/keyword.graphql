type Keyword {
  id: ID!
  value: String!
  language: Language!
  videos: [Video!]!
}

type Query {
  keywords(languageId: ID): [Keyword!]!
}

extend type Video {
  keywords(languageId: ID): VideoVariant
}
