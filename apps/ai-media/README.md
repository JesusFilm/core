# AI Media

A minimal Next.js app that demonstrates the `subtitles` workflow built with Mux AI primitives and a lightweight Workflow DevKit.

## Local development

```bash
pnpm exec nx serve ai-media
```

The app runs on http://localhost:4900.
