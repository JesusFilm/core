{
  "root": "apps/api-gateway-old",
  "sourceRoot": "apps/api-gateway-old/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/api-gateway-old",
        "main": "apps/api-gateway-old/src/main.ts",
        "tsConfig": "apps/api-gateway-old/tsconfig.app.json"
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/api-gateway-old/src/environments/environment.ts",
              "with": "apps/api-gateway-old/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:execute",
      "options": {
        "buildTarget": "api-gateway-old:build"
      }
    },
    "serve-all": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": [
          {
            "command": "nx serve api-journeys"
          },
          {
            "command": "nx serve api-gateway-old"
          }
        ],
        "parallel": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "options": {
        "lintFilePatterns": ["apps/api-gateway-old/**/*.ts"]
      },
      "outputs": ["{options.outputFile}"]
    },
    "type-check": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "npx tsc -b apps/api-gateway-old/tsconfig.json"
      }
    },
    "generate-graphql": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": [
          {
            "command": "rover supergraph compose --config apps/api-gateway-old/supergraph.yml > apps/api-gateway-old/schema.graphql"
          }
        ]
      }
    }
  },
  "tags": []
}
