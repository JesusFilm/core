import { NavigationMenuItem } from './NavigationMenuItem'

describe('NavigationMenuItem', () => {
  it('renders menu item', () => {
    // icon + text
  })
  it('renders menuitem in correct color', () => {})
  it('renders menuitem with link', () => {})
  it('calls onClick', () => {})
})
