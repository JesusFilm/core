describe('formatTemplateCustomizationString', () => {
  it('should return empty string if no customization fields are provided', () => {
    // TODO TEST: returns empty string if no customization fields are provided
  })
  it('should return empty string if no customization text is provided', () => {
    // TODO TEST: returns empty string if no customization text is provided
  })
  it('should return empty string if no customization fields are provided and no customization text is provided', () => {
    // TODO TEST: returns empty string if no customization fields are provided and no customization text is provided
  })
  it('should handle missingKeys', () => {
    // TODO TEST: adds missing keys to bottom of the text
  })
})
