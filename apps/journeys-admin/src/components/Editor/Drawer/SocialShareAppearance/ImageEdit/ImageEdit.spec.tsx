import { ImageEdit } from './ImageEdit'

describe('ImageEdit', () => {
  it('should disaply placeholder icon when no image set', () => {})
  it('should display the primaryImage', () => {})
  it('create the primaryImage', () => {})
  it('update the primaryImage', () => {})
  it('delete the primaryImage', () => {})
})
