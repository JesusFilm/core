// Naming bridge: the backend entity is `TemplateGalleryPage` (Pothos /
// Prisma), the UI surface is "Collection". `TemplateGalleryPageList` is the
// component name to keep grep-ability with the schema; everything user-
// facing — labels, snackbars, dialog title — uses "Collection".
export { TemplateGalleryPageList } from './TemplateGalleryPageList'
