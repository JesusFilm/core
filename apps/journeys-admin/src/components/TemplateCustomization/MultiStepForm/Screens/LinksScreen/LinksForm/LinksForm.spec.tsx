describe('LinksForm', () => {
  it('should render links', () => {
    // TODO TEST: renders link, typog and button
  })
  it('should handle change', () => {})
  it('should handle link open', () => {})
  it('should handle email open', () => {})
})
