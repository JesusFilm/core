describe('LinksScreen', () => {
  it('should render cards preview', () => {
    // TODO TEST: renders cards preview
  })
  it('should render links form', () => {})
  it('should handle normalize url', () => {})
  it('should handle normalize email', () => {})
  it('should handle submit', () => {})
})
