type User @key(fields: "id") {
  id: ID!
  firstName: String!
  lastName: String
  email: String!
  imageUrl: String
  role: UserRole
}

enum UserRole {
  """
  User can create templates and
  add them to template library
  """
  publisher
}

extend type Query {
  me: User
}
