import type { GetStaticProps } from 'next'

import { PageToolsVisuals } from '../../../../src/components/ToolsVisuals/PageToolsVisuals'

export const getStaticProps: GetStaticProps = async () => {
  return {
    props: {}
  }
}

export default PageToolsVisuals
