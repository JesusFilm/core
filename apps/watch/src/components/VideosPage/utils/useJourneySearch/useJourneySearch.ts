// potential file for useJourneySearch

export function useJourneySearch(): string {
  return 'dummy function'
}

/* 
 it should return data from algolia when it gets passed a title
 it should return data from algolia when it gets passed a languageId or languagesIds
 it should return data from algolia when it gets passed a category or categories


 Potential steps:
Need to mimic current useVideoSearch:
 - some version of getQueryParameters ? 
 - some version of checkfilterapplied? 
 - some version of transformAlgoliaVideos? 
*/
