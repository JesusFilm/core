{
  "name": "resources-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/resources-e2e/src",
  "projectType": "application",
  "tags": [],
  "implicitDependencies": ["resources"],
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["playwright test"],
        "cwd": "apps/resources-e2e"
      }
    },
    "update-snapshots": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["playwright test --update-snapshots"],
        "cwd": "apps/resources-e2e"
      }
    },
    "debug": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "PWDEBUG=1 xvfb-run playwright test --project=chromium --timeout=0"
        ],
        "cwd": "apps/resources-e2e"
      }
    },
    "show-report": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["playwright show-report"],
        "cwd": "apps/resources-e2e"
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -b apps/resources-e2e/tsconfig.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "cache": true,
        "cacheLocation": ".cache/resources-e2e/eslint",
        "cacheStrategy": "content"
      }
    }
  }
}
