interface Block {
  id: ID!
  journeyId: ID!
  parentBlockId: ID
  parentOrder: Int!
}

extend type Journey {
  blocks: [Block!]
}

extend type Mutation {
  blockOrderUpdate(id: ID!, journeyId: ID!, parentOrder: Int!): [Block!]!
  blockRemove(id: ID!, journeyId: ID!): [Block!]!
}
