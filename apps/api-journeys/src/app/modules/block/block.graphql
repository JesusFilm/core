interface Block {
  id: ID!
  journeyId: ID!
  parentBlockId: ID
  parentOrder: Int
}

extend type Journey {
  blocks: [Block!]
}

extend type Mutation {
  blockDelete(id: ID!, parentBlockIdjourneyId: ID!): [Block!]!
  blockOrderUpdate(id: ID!, journeyId: ID!, parentOrder: Int!): [Block!]!
}
