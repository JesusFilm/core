type RadioOptionBlock implements Block {
  id: ID!
  type: String!
  parentBlockId: ID
  label: String!
  action: Action
}

type RadioQuestionBlock implements Block {
  id: ID!
  type: String!
  parentBlockId: ID
  label: String!
  description: String
}