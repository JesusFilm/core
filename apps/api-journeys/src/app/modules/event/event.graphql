interface Event {
  id: ID!
  visitorId: ID!
  journeyId: ID!
  createdAt: DateTime!
  blockId: ID
  stepId: ID
  label: String
  """
  TODO: check if VideoBlockSource enum can be type String
  """
  value: String
}

extend type Query {
  visitorEvents(visitorId: String!): [Event!]!
}
