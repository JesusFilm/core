# PNPM Configuration
# Enable hoisting to make workspace dependencies accessible from root
# This allows NX commands to access dependencies from any workspace

# Hoist all dependencies to the root node_modules
shamefully-hoist=true

# Use the same node-linker as default
node-linker=hoisted



