# Project Instructions

All conventions and path-scoped rules live in `AGENTS.md` files (model-agnostic).

- Root conventions (code style, branch naming, testing, agent workflow): see the repo-root `AGENTS.md`.
- Path-scoped rules: read the `AGENTS.md` in — or nearest above — the directory you are editing (e.g. `apis/AGENTS.md`, `apps/AGENTS.md`, `apps/<app>/AGENTS.md`, `workers/AGENTS.md`, `infrastructure/AGENTS.md`).

@../AGENTS.md
